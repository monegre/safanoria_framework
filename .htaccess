# This file will redirect you to the installer
# 
# If Safanòria is already installed you should not be seeing this file
#
# Instead, you should place this (remove #comments!):
#
# <IfModule mod_rewrite.c>
#     RewriteEngine on
#	  RewriteBase /
#     RewriteRule    ^$    public/    [L]
#     RewriteRule    (.*) public/$1   [L]
# </IfModule>

#<IfModule mod_rewrite.c>
#    RewriteEngine on
# 	 RewriteBase /
#    RewriteRule ^$ install/ [L]
#</IfModule>

 <IfModule mod_rewrite.c>
     RewriteEngine on
	  RewriteBase /
     RewriteRule    ^$    public/    [L]
     RewriteRule    (.*) public/$1   [L]
 </IfModule>